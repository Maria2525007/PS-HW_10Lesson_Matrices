# Matrices

"Product Star" course homework (external, not ITMO).

`Vector` and `Matrix` implemented from scratch over `int[]`: add, subtract,
scalar multiply, dot product, vector length; matrix multiply, transpose,
determinant. No external math library.
